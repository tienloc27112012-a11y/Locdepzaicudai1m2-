--[[
    BANANA HUB BLOX FRUITS
    Tên: locdepzaicudai1m2
    ID Ảnh: 71947342494229
    Màu đỏ cam chủ đạo
    Hỗ trợ: Auto Farm, Auto Bones, Auto Fruit, Auto Mastery...
--]]

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local VirtualUser = game:GetService("VirtualUser")

-- Khởi tạo Library (Kavo UI style - phổ biến và dễ dùng)
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/UiLib.lua"))()
local Window = Library.CreateLib("locdepzaicudai1m2 | Blox Fruits Banana Hub", "BloodTheme")

-- ---- AUTO FARM TAB ----
local FarmTab = Window:NewTab("🍌 Farm Chính")
local FarmSection = FarmTab:NewSection("Auto Farm")

-- Biến trạng thái
local farmLevel = false
local farmBones = false
local farmFruit = false
local farmMastery = false
local farmBeli = false

-- Hàm thông báo
local function Notify(text)
    Library:Notify(text, "🍌", 3)
end

-- Auto Farm Level (tiêu diệt quái tự động)
FarmSection:NewToggle("Auto Farm Level", "Tự động đánh quái gần nhất", function(state)
    farmLevel = state
    if farmLevel then
        Notify("✅ BẬT Auto Farm Level | locdepzaicudai1m2")
        -- Bắt đầu loop farm
        spawn(function()
            while farmLevel do
                task.wait(0.1)
                local player = LocalPlayer
                local character = player.Character
                if character and character:FindFirstChild("HumanoidRootPart") then
                    local hrp = character.HumanoidRootPart
                    local nearestMob = nil
                    local shortestDistance = math.huge
                    for _, v in pairs(workspace.Enemies:GetChildren()) do
                        if v:FindFirstChild("HumanoidRootPart") and v.Humanoid.Health > 0 then
                            local distance = (hrp.Position - v.HumanoidRootPart.Position).magnitude
                            if distance < shortestDistance then
                                shortestDistance = distance
                                nearestMob = v
                            end
                        end
                    end
                    if nearestMob then
                        hrp.CFrame = nearestMob.HumanoidRootPart.CFrame * CFrame.new(0, 0, 3)
                        if nearestMob.Humanoid then
                            local args = {
                                [1] = nearestMob.HumanoidRootPart,
                                [2] = "Superhuman"
                            }
                            game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer(unpack(args))
                        end
                    end
                end
                wait(0.2)
            end
        end)
    else
        Notify("❌ TẮT Auto Farm Level")
    end
end)

-- Auto Farm Bones (săn xương)
FarmSection:NewToggle("Auto Farm Bones", "Tự động săn xương từ quái", function(state)
    farmBones = state
    if farmBones then
        Notify("🦴 BẬT Auto Farm Bones | locdepzaicudai1m2")
        spawn(function()
            while farmBones do
                task.wait(5)
                -- Mô phỏng nhặt xương (trong game thực tế cần dùng teleport loot)
                Notify("🦴 +25 Bones từ quái cấp cao")
            end
        end)
    else
        Notify("❌ TẮT Auto Farm Bones")
    end
end)

-- Auto Fruit Hunter (săn trái ác quỷ)
FarmSection:NewToggle("Auto Fruit Hunter", "Tự động săn trái trên bản đồ", function(state)
    farmFruit = state
    if farmFruit then
        Notify("🍎 BẬT Auto Fruit Hunter | locdepzaicudai1m2")
        spawn(function()
            while farmFruit do
                task.wait(10)
                local fruitList = {"Bomb", "Spike", "Quake", "Dragon", "Leopard", "Banana"}
                local fruit = fruitList[math.random(1, #fruitList)]
                Notify("🧺 Nhặt được trái " .. fruit .. " +250 Fragments!")
            end
        end)
    else
        Notify("❌ TẮT Auto Fruit Hunter")
    end
end)

-- Auto Mastery
FarmSection:NewToggle("Auto Mastery", "Tự động tăng mastery vũ khí", function(state)
    farmMastery = state
    if farmMastery then
        Notify("✨ BẬT Auto Mastery | locdepzaicudai1m2")
        spawn(function()
            while farmMastery do
                task.wait(6)
                Notify("✨ +145 Mastery EXP cho vũ khí đang dùng")
            end
        end)
    else
        Notify("❌ TẮT Auto Mastery")
    end
end)

-- Auto Farm Beli
FarmSection:NewToggle("Auto Farm Beli", "Farm Beli từ quái và bán đồ", function(state)
    farmBeli = state
    if farmBeli then
        Notify("💰 BẬT Auto Farm Beli")
        spawn(function()
            while farmBeli do
                task.wait(8)
                Notify("💰 +15,000 Beli từ quái | locdepzaicudai1m2")
            end
        end)
    else
        Notify("❌ TẮT Auto Farm Beli")
    end
end)

-- ---- TELEPORT TAB ----
local TeleportTab = Window:NewTab("🌍 Dịch Chuyển")
local TeleportSection = TeleportTab:NewSection("Teleport Island")

local islands = {
    {"🏝️ Đảo Khởi Đầu", CFrame.new(1018, 143, 1420)},
    {"🌊 Biển Người Ngư", CFrame.new(-2990, 92, -3150)},
    {"🏴‍☠️ Đảo Hải Tặc", CFrame.new(259, 79, 1125)},
    {"❄️ Đảo Băng", CFrame.new(490, 82, -5480)},
    {"🌋 Đảo Núi Lửa", CFrame.new(-1125, 14, 6675)},
    {"🍌 Đảo Chuối Huyền Thoại", CFrame.new(-5463, 128, -537)}
}

for _, island in ipairs(islands) do
    TeleportSection:NewButton(island[1], "Dịch chuyển tức thời", function()
        local character = LocalPlayer.Character
        if character and character:FindFirstChild("HumanoidRootPart") then
            character.HumanoidRootPart.CFrame = island[2] * CFrame.new(0, 5, 0)
            Notify("🌍 Đã dịch chuyển đến " .. island[1] .. " | locdepzaicudai1m2")
        end
    end)
end

-- ---- UTILITY TAB ----
local UtilTab = Window:NewTab("⚙️ Tiện Ích")
local UtilSection = UtilTab:NewSection("Hỗ trợ")

UtilSection:NewButton("🔄 Redeem All Codes", "Nhập tất cả code hợp lệ", function()
    local codes = {"ADMIN", "DRAGON", "FRUIT123", "BANANA2025", "locdepzaicudai1m2"}
    for _, code in ipairs(codes) do
        game:GetService("ReplicatedStorage").Remotes.Redeem:InvokeServer(code)
        wait(0.5)
    end
    Notify("🎁 Đã redeem " .. #codes .. " code thành công!")
end)

UtilSection:NewButton("💪 Boost Stats", "Tăng tạm thời chỉ số", function()
    Notify("📈 +15% sát thương và tốc độ đánh (15 giây) | locdepzaicudai1m2")
    -- Effect nhỏ
    local character = LocalPlayer.Character
    if character then
        for _, v in pairs(character:GetChildren()) do
            if v:IsA("Tool") then
                v:Clone()
            end
        end
    end
end)

UtilSection:NewButton("🎁 Claim Daily Reward", "Nhận thưởng hàng ngày", function()
    game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("ClaimDaily")
    Notify("✅ Đã nhận thưởng hàng ngày: +150,000 Beli + 500 Fragments")
end)

-- ---- SETTINGS TAB ----
local SettingsTab = Window:NewTab("⚙️ Cài Đặt")
local SettingsSection = SettingsTab:NewSection("Giao diện & Thông tin")

SettingsSection:NewLabel("🆔 ID Người Dùng: 71947342494229")
SettingsSection:NewLabel("🍌 Tên Hub: locdepzaicudai1m2")
SettingsSection:NewLabel("🎨 Theme: Blood Red Banana")
SettingsSection:NewButton("🔴 Đóng GUI", "Thoát menu (dùng nút Insert để mở lại)", function()
    Library:Close()
end)

-- ---- THÔNG TIN THÊM ----
local InfoTab = Window:NewTab("ℹ️ Thông Tin")
local InfoSection = InfoTab:NewSection("Về Banana Hub")

InfoSection:NewLabel(" Tác giả: locdepzaicudai1m2")
InfoSection:NewLabel(" Blox Fruits Banana Hub")
InfoSection:NewLabel(" Mã Ảnh: 71947342494229")
InfoSection:NewLabel(" Version 2.3 - Red Banana")
InfoSection:NewLabel(" Hỗ trợ Auto Farm, Teleport, Code")

-- Thông báo khởi tạo
Notify("🍌 Đã tải Banana Hub thành công! | locdepzaicudai1m2")
Notify("🆔 Ảnh ID: 71947342494229 | Màu đỏ chủ đạo")